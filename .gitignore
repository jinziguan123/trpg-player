node_modules/
dist/
build/
.vite/
*.tsbuildinfo

__pycache__/
*.py[cod]
*.egg-info/
.venv/
*.db
*.db-wal
*.db-shm

# 用户上传的地图素材（运行时文件，不入库）
server/data/
# 打包用的内置种子（由 make_seed.py 从开发库生成，构建时重建，不入库）
server/seed/

.env
# 含 AI 密钥，不入库（密钥应从 .env / 设置页注入）
ai_settings.json
.DS_Store
target/
.claude/
.worktrees/

# Tauri 打包：Rust 产物、生成的 schema、以及按平台构建的 sidecar 二进制（由脚本重建）
src-tauri/gen/
src-tauri/binaries/
