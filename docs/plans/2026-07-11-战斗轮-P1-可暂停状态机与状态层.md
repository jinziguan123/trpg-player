# 战斗轮 P1：可暂停状态机 + 声明式状态层 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 把战斗防御从「引擎替玩家猜」改成「被攻击时真人当场抉择」，根治「闪避成功攻击者却扣血」（⑤）与「玩家无从选择防御」（④），并把角色状态规则从 `apply_damage` 内联中抽成纯函数、补濒死每轮 tick。

**Architecture:** 战斗态 `world_state.combat` 增 `pending_reaction` 字段：当非真人行动者攻击一个真人防御者时，`drive_npcs` **暂停**（落 pending + 广播 `combat_reaction_prompt` + return），新端点 `POST /combat/reaction` 收到真人选择后结算这一击、清 pending、**续跑**。NPC 防御者仍自动选择（引擎 `heuristic_defense`）。状态规则（重伤/濒死/死亡）抽进纯 `combat.py`，落库/写回角色卡留 `combat_service`。

**Tech Stack:** 后端 FastAPI + SQLAlchemy；纯规则引擎 `app/rules/coc/combat.py`（掷骰经 `roll`/`resolve_skill_check`，测试 monkeypatch 钉死）；pytest；前端 React + TS（`CombatPanel.tsx`）。

**设计依据：** [2026-07-11-战斗轮重构-design.md](2026-07-11-战斗轮重构-design.md) §3/§4。

**通用纪律：** 每个 Task 走 TDD（先写失败测试 → 跑红 → 最小实现 → 跑绿 → commit）。后端每次 commit 前 `cd server && .venv/bin/pytest -q && .venv/bin/ruff check .`；前端改动后 `cd apps/web && npx tsc --noEmit && npx vite build`。图标只用 `react-icons/gi`，禁 emoji。commit 中文 + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

## Part A — 引擎层（纯函数，`app/rules/coc/combat.py`）

### Task 1：`allowed_reactions(is_firearm)` — 火器禁反击

**Files:**
- Modify: `server/app/rules/coc/combat.py`（在 `resolve_attack` 上方加）
- Test: `server/tests/test_combat.py`

**Step 1 — 写失败测试：**
```python
def test_allowed_reactions_firearm_excludes_fight_back():
    from app.rules.coc.combat import allowed_reactions
    assert allowed_reactions(is_firearm=False) == ["fight_back", "dodge"]
    assert allowed_reactions(is_firearm=True) == ["dodge", "cover"]
```

**Step 2 — 跑红：** `cd server && .venv/bin/pytest tests/test_combat.py::test_allowed_reactions_firearm_excludes_fight_back -q` → ImportError。

**Step 3 — 最小实现：**
```python
def allowed_reactions(is_firearm: bool) -> list[str]:
    """被攻击者可选的反应。火器不能反击（RAW），只能闪避/扑掩体。"""
    return ["dodge", "cover"] if is_firearm else ["fight_back", "dodge"]
```

**Step 4 — 跑绿。Step 5 — commit：** `feat(combat): allowed_reactions 火器禁反击`

---

### Task 2：`resolve_attack` 支持 `defense="cover"` + 火器闪避转困难

**背景：** 现在 `ranged or defense is None` 时攻方达标即命中，被火器攻击**连闪避都没有**。改为：远程 + `dodge/cover` → 射手转**困难难度**（扑向掩体），命中即伤、无反击；远程 + 无反应仍达标即命中。近战分支不变。

**Files:**
- Modify: `server/app/rules/coc/combat.py`（`resolve_attack` 远程分支，约 [:192](../../server/app/rules/coc/combat.py)）
- Test: `server/tests/test_combat.py`

**Step 1 — 写失败测试**（monkeypatch 钉死骰点）：
```python
def test_resolve_attack_firearm_cover_raises_difficulty(monkeypatch):
    import app.rules.coc.checks as checks
    from app.rules.coc import combat
    # 手枪技能 50：普通难度目标 50、困难难度目标 25。钉死掷出 40：普通命中、困难失手。
    monkeypatch.setattr(checks, "roll_percentile", lambda: 40)
    shooter = {"skills": {"射击(手枪)": 50}, "base_attributes": {}, "system_data": {}}
    res = combat.resolve_attack(shooter, "0", "手枪",
                                defender_data={"skills": {}, "base_attributes": {}, "system_data": {}},
                                defense="cover", ranged=True)
    assert res["hit"] is False           # 扑掩体把射击压到困难，40 > 25 → 失手
    assert res["damage_to"] is None
```

**Step 2 — 跑红**（`cover` 未被处理，仍走「达标即命中」→ hit True）。

**Step 3 — 实现**：把远程分支从
```python
    if ranged or defense is None:
        if atk.meets_difficulty:
            ...
        return result
```
改为（注意 `atk` 在函数顶部已按 `difficulty` 掷过——远程+掩体需要用更高难度**重掷**判定命中）：
```python
    # 远程：无反应 → 达标即命中；扑掩体/闪避 → 射手转困难难度（扑向掩体），无反击
    if ranged:
        if defense in ("dodge", "cover"):
            atk = resolve_skill_check(attacker_data, atk_skill, "hard")
            result["attacker_check"] = atk
        if atk.meets_difficulty:
            result["hit"] = True
            result["damage"] = _damage(atk.tier)
            result["damage_to"] = "defender"
        return result
    if defense is None:
        if atk.meets_difficulty:
            result["hit"] = True
            result["damage"] = _damage(atk.tier)
            result["damage_to"] = "defender"
        return result
```
并在近战 `dodge` 分支同样接受 `defense == "cover"`（近战扑掩体等同闪避）：把 `if defense == "dodge":` 改为 `if defense in ("dodge", "cover"):`。

**Step 4 — 跑绿**（连同既有 test_combat 全绿，确保近战/远程既有断言不破）。**Step 5 — commit：** `feat(combat): resolve_attack 支持扑掩体+火器闪避转困难`

---

### Task 3：`heuristic_defense(defender, is_firearm)` — NPC 防御者自动选择

**背景：** 删 `_default_defense` 后，**NPC 作为防御方**（真人攻击 NPC 时）仍需一个防御选择。真人防御方走交互，不经此。

**Files:** Modify `combat.py`；Test `tests/test_combat.py`

**Step 1 — 失败测试：**
```python
def test_heuristic_defense_firearm_never_fight_back():
    from app.rules.coc.combat import heuristic_defense
    aggressive = {"combat_ai": "aggressive"}
    assert heuristic_defense(aggressive, is_firearm=True) in ("dodge", "cover")
    assert heuristic_defense(aggressive, is_firearm=False) == "fight_back"
    assert heuristic_defense({"combat_ai": "cautious"}, is_firearm=False) == "dodge"
```

**Step 3 — 实现：**
```python
def heuristic_defense(defender: dict, is_firearm: bool) -> str:
    """NPC 防御者的自动防御选择（真人防御走交互，不经此）。
    火器只能闪避；近战：好斗者反击，其余闪避。"""
    if is_firearm:
        return "dodge"
    return "fight_back" if defender.get("combat_ai") == "aggressive" else "dodge"
```

**Step 5 — commit：** `feat(combat): heuristic_defense NPC 防御者自动选择`

---

### Task 4：状态规则抽取 — `resolve_wound(...)` 纯函数

**背景：** 把 `apply_damage`（[combat_service.py:159](../../server/app/services/combat_service.py)）里内联的「新 HP / 重伤 / 濒死 / 死亡 / 重伤 CON 检定→昏迷」抽成纯函数（可 monkeypatch 骰子、可单测），落库/写角色卡仍留 service。

**Files:** Modify `combat.py`；Test `tests/test_combat.py`

**Step 1 — 失败测试：**
```python
def test_resolve_wound_major_wound_triggers_con_check(monkeypatch):
    import app.rules.coc.checks as checks
    from app.rules.coc import combat
    monkeypatch.setattr(checks, "roll_percentile", lambda: 99)  # 体质检定必失败
    target = {"skills": {}, "base_attributes": {"CON": 50}, "system_data": {}}
    r = combat.resolve_wound(hp=10, max_hp=13, damage=7, defender_data=target)
    assert r["new_hp"] == 3
    assert r["status"] == "unconscious"          # 单击≥半血(7≥6) + CON 失败 → 昏迷
    assert any("重伤" in line for line in r["lines"])

def test_resolve_wound_minor_hit_no_status_change():
    from app.rules.coc import combat
    r = combat.resolve_wound(hp=10, max_hp=13, damage=2, defender_data={})
    assert r["new_hp"] == 8 and r["status"] == "ok"

def test_resolve_wound_zero_hp_is_dying():
    from app.rules.coc import combat
    r = combat.resolve_wound(hp=3, max_hp=13, damage=5, defender_data={})
    assert r["new_hp"] == 0 and r["status"] == "dying"

def test_resolve_wound_overkill_is_dead():
    from app.rules.coc import combat
    r = combat.resolve_wound(hp=1, max_hp=13, damage=99, defender_data={})
    assert r["status"] == "dead"
```

**Step 3 — 实现**（把 service 里那段规则搬来、去掉 DB 交互）：
```python
def resolve_wound(hp: int, max_hp: int, damage: int, defender_data: dict) -> dict:
    """结算一次伤害的 HP 与状态迁移（纯规则，不碰 DB）。
    返回 {new_hp, status, lines}。重伤（单击≥半血）触发 CON 检定，失败则昏迷。"""
    max_hp = max_hp or 1
    raw = hp - damage
    new_hp = max(0, raw)
    lines = [f"受到 {damage} 点伤害（HP {hp}→{new_hp}）"]
    major = damage >= max_hp // 2 and max_hp > 0
    if raw <= -max_hp:
        return {"new_hp": new_hp, "status": "dead", "lines": lines + ["当场毙命！"]}
    if new_hp <= 0:
        return {"new_hp": 0, "status": "dying", "lines": lines + ["濒死，需急救/医学稳定。"]}
    if major:
        con = resolve_skill_check(defender_data, "体质", "normal")
        lines.append(f"重伤体质检定：{con.description}")
        if con.outcome in ("failure", "fumble"):
            return {"new_hp": new_hp, "status": "unconscious", "lines": lines + ["眼前一黑，昏迷倒地！"]}
        return {"new_hp": new_hp, "status": "major_wound", "lines": lines}
    return {"new_hp": new_hp, "status": "ok", "lines": lines}
```

**Step 5 — commit：** `feat(combat): resolve_wound 抽出伤害/重伤/濒死纯规则`

---

### Task 5：`tick_dying(participant, defender_data)` — 濒死每轮 CON

**背景：** 濒死者每轮开始掷 CON，失败则死亡（P1 先做「恶化到死」；急救稳住留 P2）。

**Files:** Modify `combat.py`；Test `tests/test_combat.py`

**Step 1 — 失败测试：**
```python
def test_tick_dying_con_fail_dies(monkeypatch):
    import app.rules.coc.checks as checks
    from app.rules.coc import combat
    monkeypatch.setattr(checks, "roll_percentile", lambda: 99)
    p = {"status": "dying", "base_attributes": {"CON": 50}, "skills": {}, "system_data": {}}
    lines = combat.tick_dying(p)
    assert p["status"] == "dead" and lines

def test_tick_dying_con_pass_holds(monkeypatch):
    import app.rules.coc.checks as checks
    from app.rules.coc import combat
    monkeypatch.setattr(checks, "roll_percentile", lambda: 1)
    p = {"status": "dying", "base_attributes": {"CON": 50}, "skills": {}, "system_data": {}}
    combat.tick_dying(p)
    assert p["status"] == "dying"

def test_tick_dying_noop_when_not_dying():
    from app.rules.coc import combat
    p = {"status": "ok"}
    assert combat.tick_dying(p) == []
```

**Step 3 — 实现：**
```python
def tick_dying(participant: dict) -> list[str]:
    """濒死者每轮开始的 CON 检定：失败则死亡。非濒死者 no-op。原地改 participant。"""
    if participant.get("status") != "dying":
        return []
    data = {"skills": participant.get("skills") or {},
            "base_attributes": participant.get("base_attributes") or {},
            "system_data": participant.get("system_data") or {}}
    con = resolve_skill_check(data, "体质", "normal")
    if con.outcome in ("failure", "fumble"):
        participant["status"] = "dead"
        return [f"{participant.get('name', '')}｜濒死体质检定失败（{con.description}），气绝身亡。"]
    return [f"{participant.get('name', '')}｜濒死体质检定{con.description}，暂时挺住。"]
```

**Step 5 — commit：** `feat(combat): tick_dying 濒死每轮体质检定`

---

## Part B — 状态机层（`app/services/combat_service.py`）

### Task 6：`apply_damage` 改用 `resolve_wound`；删 `_default_defense`

**Files:** Modify `combat_service.py`（[apply_damage:159](../../server/app/services/combat_service.py)、删 `_default_defense:285`）；Test `tests/test_combat_service.py`

**Step 1 — 失败测试**（锁定行为不回退 + 引用点已改）：
```python
def test_apply_damage_uses_resolve_wound(monkeypatch, db_session, combat_state_factory):
    # 复用既有 fixture；断言重伤昏迷仍生效、HP 写回角色卡（行为等价重构）
    ...  # 若无现成 fixture，参照 test_combat_service 既有用例改造
```
（若既有 `test_combat_service.py` 已覆盖 apply_damage 的重伤/昏迷路径，则**不新增测试**，仅确保重构后既有断言全绿即可——这是等价重构。）

**Step 3 — 实现：**
- `apply_damage` 内把「算 new / major / dead / dying / CON」整段替换为：
```python
    r = engine.resolve_wound(target.get("hp", 0), target.get("max_hp") or 1, amount, _char_data(target))
    target["hp"] = r["new_hp"]
    target["status"] = r["status"]
    lines = [f"{target['name']} {line}" if not line.startswith(target['name']) else line
             for line in r["lines"]]
    if reason and lines:
        lines[0] += f"——{reason}"
```
  （保持返回 `lines` 与写回角色卡逻辑不变。）
- 删除 `_default_defense` 函数及其在 `_apply_one_action` 的调用（Task 7 用新逻辑取代）。

**Step 4 — 跑绿**（`test_combat_service.py` 既有断言全过）。**Step 5 — commit：** `refactor(combat): apply_damage 走 resolve_wound + 删 _default_defense`

---

### Task 7：`drive_npcs` 检测「NPC 攻击真人」→ 暂停等反应

**Files:** Modify `combat_service.py`（`drive_npcs:290`、`_apply_one_action` attack 分支）；Test `tests/test_combat_service.py`

**Step 1 — 失败测试：**
```python
@pytest.mark.asyncio
async def test_drive_npcs_pauses_when_npc_attacks_human(db_session, ...):
    # 构造：先攻序里 NPC 在前、真人在后；NPC 启发式攻击真人
    # 期望：drive 后 state["pending_reaction"] 非空、defender 是真人、未结算伤害、广播含 combat_reaction_prompt
    state = ...
    chunks, beats = await combat_service.drive_npcs(db_session, sid, state, agent=None)
    pr = state.get("pending_reaction")
    assert pr and pr["defender_id"] == human_id
    assert any('"combat_reaction_prompt"' in c for c in chunks)
```

**Step 3 — 实现：** 在 `drive_npcs` 循环里，取得 `action` 后、`_apply_one_action` 之前插入拦截：
```python
        action = None
        if agent and actor.get("is_key"):
            action = _validate_npc_action(state, actor, await agent.decide(state, actor, scene_hint))
        if action is None:
            action = engine.heuristic_npc_action(state, actor)

        # 攻击一个真人存活防御者 → 暂停，等真人选反应（不结算、不推进）
        atype = action.get("type") or action.get("action")
        if atype == "attack":
            target = _find(state, action.get("target_id"))
            if target and engine.is_active(target) and target.get("is_human"):
                weapon = action.get("weapon") or actor.get("weapon") or "徒手格斗"
                is_fire = _weapon_is_firearm(weapon)
                state["pending_reaction"] = {
                    "attacker_id": actor["id"], "defender_id": target["id"],
                    "weapon": weapon, "ranged": is_fire,
                    "allowed": engine.allowed_reactions(is_fire),
                }
                _save_combat(db, session_id, state)
                chunks.append(_chunk("combat_reaction_prompt", metadata={
                    **state["pending_reaction"],
                    "attacker_name": actor["name"], "defender_name": target["name"],
                }))
                return chunks, beats   # 暂停驱动
        c, s = _apply_one_action(db, session_id, state, actor, action)
        ...
```
（NPC 攻击 NPC、真人攻击等其余路径不变。）

**Step 5 — commit：** `feat(combat): NPC 攻击真人时暂停等交互反应`

---

### Task 8：`resolve_reaction(...)` — 结算暂停的攻击并续跑

**Files:** Modify `combat_service.py`；Test `tests/test_combat_service.py`

**Step 1 — 失败测试**（含 ⑤ 回归：闪避成功攻击者不掉血）：
```python
@pytest.mark.asyncio
async def test_resolve_reaction_dodge_success_no_damage_to_attacker(db_session, ...):
    # pending_reaction 已置（NPC 攻真人）；真人选 dodge 且闪避成功
    # 期望：attacker HP 不变、pending 清空、轮次推进、驱动续跑
    chunks = await combat_service.resolve_reaction(db_session, sid, human_id, "dodge", None, agent=None)
    assert attacker["hp"] == attacker_hp_before      # ⑤：闪避永不伤攻击者
    assert state.get("pending_reaction") is None
```

**Step 3 — 实现：**
```python
async def resolve_reaction(db, session_id, defender_id, choice, weapon=None,
                           agent=None, scene_hint=""):
    session = db.get(GameSession, session_id)
    state = get_combat(session)
    if not state:
        raise ValueError("当前不在战斗中")
    pr = state.get("pending_reaction")
    if not pr or pr["defender_id"] != defender_id:
        raise ValueError("现在没有等待你的反应")
    if choice not in pr["allowed"]:
        raise ValueError("该反应在本次攻击下不可用")
    attacker = _find(state, pr["attacker_id"])
    defender = _find(state, pr["defender_id"])
    out: list[str] = []
    beats: list[str] = []
    if attacker and defender and engine.is_active(attacker) and engine.is_active(defender):
        res = engine.resolve_attack(
            _char_data(attacker), attacker.get("db", "0"), pr["weapon"],
            defender_data=_char_data(defender), defense=choice, ranged=pr["ranged"],
        )
        out.append(_combat_dice(db, session_id, attacker, defender, pr["weapon"], res))
        if res["hit"] and res["damage"]:
            victim = defender if res["damage_to"] == "defender" else attacker
            for line in apply_damage(db, state, victim, res["damage"]["total"],
                                     reason=f"{attacker['name']} 的 {pr['weapon']}"):
                out.append(_combat_line(db, session_id, line))
        beats.append(_reaction_beat(attacker, defender, choice, res))
    state["pending_reaction"] = None
    if attacker:
        attacker["acted_this_round"] = True
    engine.advance_turn(state)
    drive_chunks, drive_beats = await drive_npcs(db, session_id, state, agent, scene_hint)
    beats += drive_beats
    narr = []
    if agent and beats:
        prose = await agent.narrate(state, beats, scene_hint)
        if prose:
            narr.append(_combat_narration(db, session_id, prose))
    return narr + out + drive_chunks
```
（`_reaction_beat` 小工具产一句机械摘要供子代理叙述。）

**Step 5 — commit：** `feat(combat): resolve_reaction 结算交互反应并续跑（修 ④⑤）`

---

### Task 9：濒死每轮 tick 接进回合推进

**Files:** Modify `combat_service.py`（`drive_npcs` 每个行动者回合开始处、或 `resolve_player_action`/`resolve_reaction` 轮到濒死者时）；Test 同上文件

**实现：** 在 `drive_npcs` 循环里，`actor = current_actor(state)` 之后、若 `actor["status"] == "dying"` → `lines = engine.tick_dying(actor)`；广播这些行、若转 dead 则 `advance_turn` 跳过。真人濒死者同理在其回合开始 tick。补一个 `test_dying_participant_ticks_each_round`。

**commit：** `feat(combat): 濒死者每轮回合开始体质 tick`

---

## Part C — API 层

### Task 10：`ReactionRequest` schema + `POST /combat/reaction`

**Files:**
- Modify: `server/app/schemas/combat.py`
- Modify: `server/app/api/combat.py`
- Test: `server/tests/test_combat_service.py` 或新增 `test_combat_api` 用例

**Step 3 — 实现：**
`schemas/combat.py` 加：
```python
class ReactionRequest(BaseModel):
    """被攻击时的反应。choice: fight_back | dodge | cover。"""
    choice: str
    weapon: str | None = None
```
`api/combat.py` 加端点（仿 `combat_action`，校验 token→char_id 即 defender）：
```python
@router.post("/{session_id}/combat/reaction")
async def combat_reaction(session_id: str, data: ReactionRequest,
                          db: Session = Depends(get_db), token: str | None = Depends(player_token)):
    session = session_service.get_session(db, session_id)
    if not session:
        raise HTTPException(404, "会话不存在")
    actor_id = _actor_char_id(db, session_id, token) or session.player_character_id
    if not actor_id:
        raise HTTPException(403, "无法确定行动角色")
    try:
        chunks = await combat_service.resolve_reaction(
            db, session_id, actor_id, data.choice, data.weapon, agent=_combat_agent(db, session))
    except ValueError as e:
        raise HTTPException(409, str(e))
    for chunk in chunks:
        room_hub.broadcast(session_id, chunk)
    return {"ok": True}
```
`GET /combat` 的 `_combat_meta` 输出加上 `pending_reaction`（供断线重连恢复反应提示）。

**commit：** `feat(combat): POST /combat/reaction 端点 + 断线恢复 pending`

---

## Part D — 前端最小闭环

### Task 11：反应提示按钮（CombatPanel）

**Files:**
- Modify: `apps/web/src/pages/GameSessionPage.tsx`（`combat_reaction_prompt` chunk → 存 pending 反应态；`combat_state`/`combat_end` 清）
- Modify: `apps/web/src/components/game/CombatPanel.tsx`（pending 非空且 defender==我 → 显示 `反击[火器禁用]/闪避/扑掩体` 按钮 → `POST /combat/reaction`）
- 图标：`GiCrossedSwords`(反击) / `GiShield`(闪避) / `GiBrickWall` 或 `GiStoneWall`(扑掩体)，全 `react-icons/gi`。

**验证（非单测）：** `cd apps/web && npx tsc --noEmit && npx vite build` 通过；随后按 `preview_*` 流程起服务、构造一场 NPC 攻击真人的战斗，确认反应提示出现、选闪避成功后攻击者不掉血（⑤ 闭环）。

**commit：** `feat(combat-ui): 被攻击时反应提示（反击/闪避/扑掩体）`

---

## 收尾校验（全绿才算 P1 完成）

- `cd server && .venv/bin/pytest -q && .venv/bin/ruff check .`
- `cd apps/web && npx tsc --noEmit && npx vite build`
- 更新 evals：战斗 RUBRIC 增「防御由防御方决定、闪避不伤攻击者」评分项（可并入 P2，若本阶段 evals fixture 未动则记 TODO）。
- 手动闭环：起一场 NPC 先攻攻击真人 → 反应提示 → 选闪避成功 → 攻击者 HP 不变、驱动续跑到下一个真人回合。

## 未决 / 交接 P2

- 主动动作集（急救/观察/擒抱缴械/装填瞄准）、grappled/disarmed 状态钩子 → P2。
- HUD 战场视图、`combat_log` 消息分流、HP 变化动画 → P3。
- 「本回合默认闪避」防 AFK 快捷开关 → 可选，交互打磨阶段。
